1) to get the output for question 1 run the command "bash q1.sh <fileName>" where <fileName> is the input file required to give as argument

2) to get the output for question 1 run the command "bash q2.sh"

